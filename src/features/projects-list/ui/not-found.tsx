import { Flex, Text } from '@radix-ui/themes';

export const NotFound = () => {
    return (
        <Flex justify="center">
            <Text size="4">Ничего не найдено</Text>
        </Flex>
    );
};
